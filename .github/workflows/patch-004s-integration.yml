name: Patch 004S integration
on:
  push:
    branches:
      - agent/beblog-cam-004s-machine-envelope
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/beblog-cam-004s-machine-envelope
          fetch-depth: 0
      - name: Apply 004S integration
        run: python scripts/patch-004s-integration.py
      - name: Commit integration
        shell: bash
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/App.svelte src/lib/jobPreflight.ts src/lib/JobPreflightPanel.svelte src/lib/jobGcode.ts src/lib/JobGCodePanel.svelte scripts/check-004s-contracts.mjs
          git commit -m "feat(004s): integrate machine envelope into setup preflight and export"
          git push
