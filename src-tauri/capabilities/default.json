{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the BeBlog CAM desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
