import fs from 'node:fs';

const read=(path)=>fs.readFileSync(new URL(`../${path}`,import.meta.url),'utf8');
const requireText=(text,needle,label)=>{if(!text.includes(needle))throw new Error(`${label}: missing ${needle}`);};
const rejectText=(text,needle,label)=>{if(text.includes(needle))throw new Error(`${label}: forbidden ${needle}`);};

const canonical=read('src/lib/canonicalToolpath.ts');
const state=read('src/lib/pocketOperationState.ts');
const region=read('src/lib/regionPocketToolpath.ts');
const dxfRegion=read('src/lib/dxfRegionPocket.ts');
const stepRegion=read('src/lib/stepRegionPocket.ts');
const geometryView=read('src/lib/GeometryView.svelte');
const active=read('src/lib/activeCanonicalToolpath.ts');
const preflight=read('src/lib/jobPreflight.ts');
const step=read('src/lib/stepPocketOperation.ts');
const dxf=read('src/lib/pocketGcode.ts');
const app=read('src/App.svelte');
const persistence=read('src/lib/projectPersistence.ts');

requireText(canonical,'sourceOperationId?:string','canonical pocket source identity');
requireText(canonical,'targetKey?:string','canonical pocket target identity');
requireText(state,'export function buildPocketOperationState','shared pocket operation state');
requireText(state,'applyPocketStockAwareRoughing','shared state applies stock-aware roughing');
requireText(state,'applyPocketRestMachining','shared state applies rest machining');
requireText(state,"if(operation.strategy!=='auto')",'all explicit DXF pocket strategies use shared region kernel');
requireText(state,'buildDxfRegionPocket','DXF region strategies use shared region kernel');
requireText(state,'buildStepRegionPocket','all STEP pocket strategies use shared region kernel');
requireText(state,"operation.strategy==='auto'?(geometryState.selected.islands.length?'parallel':'concentric'):operation.strategy",'STEP auto resolves to shared region strategy');
requireText(state,'candidate.sourceOperationId===operation.restFromOperationId','rest source is selected by exact operation identity');
requireText(state,"previous.targetKey!==key",'rest machining requires identical pocket target');
requireText(state,'operation.tool.diameterMm+2*radialAllowance','radial pocket allowance changes roughing centerline');
requireText(state,'targetDepthMm-axialAllowance','axial pocket allowance leaves floor stock');
requireText(state,"entry:'plunge',stepDownMm:targetDepthMm,totalDepthMm:targetDepthMm",'finishing is a nominal single-depth pocket pass');
requireText(state,'repeatFinishRuns','finish pass count is materialized in canonical runs');
requireText(region,"export type RegionPocketStrategy='raster'|'concentric'|'parallel'",'shared region kernel owns raster, concentric and parallel strategies');
requireText(region,'function rasterPaths','shared region kernel owns raster generation');
requireText(region,'function chainPaths','shared region kernel chains safely connectable pocket paths');
requireText(region,'connectorSafe','stay-down links remain inside the pocket region');
requireText(region,'runPoints=[{...entryEnd}', 'ramp entry ends at the canonical run start');
requireText(region,'reversePath(rampPoints)','ramped material is recut at final Z before normal pocket clearing');
requireText(region,"for(const chain of chains){const entry=buildEntry",'entry is owned by each connected level chain, not every raster row');
requireText(region,"strategy==='raster'?'raster'",'shared raster emits canonical raster strategy');
requireText(region,"kind:'arc3'",'shared region strategies materialize helix entry as canonical XYZ arc');
requireText(dxfRegion,'RegionPocketStrategy','DXF region accepts every shared pocket strategy');
requireText(stepRegion,'RegionPocketStrategy','STEP region accepts every shared pocket strategy');
requireText(geometryView,'const jobEntryWorld=jobToolpaths.flatMap','STEP 3D preview consumes canonical spatial pocket entries');
requireText(geometryView,'...jobEntryWorld,...jobMotionWorld','STEP 3D preview renders entries without mutating machine motions');
requireText(app,"updatePocket({strategy:'concentric'})",'Bearbeiten exposes circle strategy');
requireText(app,"updatePocket({strategy:'parallel'})",'Bearbeiten exposes contour-parallel strategy');
requireText(app,'>Rampenwinkel <input type="number" min="0.1" max="15"','Bearbeiten exposes pocket ramp angle');
requireText(app,"updateNumber('rampAngleDeg',e)",'pocket ramp angle updates the persisted operation state');
requireText(active,'buildPocketOperationState','Bearbeiten uses shared pocket state');
requireText(active,'previousToolpaths:args.previousToolpaths','Bearbeiten forwards prior canonical operations');
requireText(preflight,"import { buildPocketOperationState } from './pocketOperationState'",'Job preflight imports shared pocket state');
requireText(preflight,'previousToolpaths:stockSimulationOperations.map(entry=>entry.toolpath)','Job preflight forwards prior accepted canonical paths');
rejectText(preflight,"import { applyPocketRestMachining }",'Job preflight must not own a second rest-machining implementation');
rejectText(preflight,"import { applyPocketStockAwareRoughing }",'Job preflight must not own a second stock-aware implementation');
requireText(step,'targetDepth=Math.max(0,-faceMachineZ)','STEP pocket depth remains derived from selected BRep floor face');
requireText(dxf,'operation.totalDepthMm','DXF pocket depth remains operation-owned');
requireText(persistence,'operationsProject','004V persistence remains operation-project owned');

console.log('004Y PASS: DXF and STEP share raster/concentric/parallel region strategies; entry is level-owned, stay-down links are region-safe, and spatial entries remain canonical through preview/preflight/NC.');
