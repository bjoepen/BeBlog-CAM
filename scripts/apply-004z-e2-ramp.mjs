import fs from 'node:fs';

function patch(path,edits){let t=fs.readFileSync(path,'utf8');for(const [from,to,label] of edits){if(!t.includes(from))throw new Error(`${path}: ${label}`);t=t.replace(from,to);}fs.writeFileSync(path,t);}

patch('src/lib/stepContourOperation.ts',[
["import { applyContourLeads } from './contourLeads';","import { applyContourLeads } from './contourLeads';\nimport { applyOpenContourRampEntry } from './openContourRampEntry';",'ramp import'],
["  const led=applyContourLeads(tabbed.toolpath,operation);errors.push(...led.errors);warnings.push(...led.warnings);if(errors.length)return fail(errors,warnings,all,chosen,eligibleSideFaceIds,effective.edgeIds);\n  if(operation.topology==='open')warnings.push(`Offene STEP-Kontur aktiv · ${effective.edgeIds.length} BRep-Kanten · ${operation.openSide==='left'?'links':operation.openSide==='right'?'rechts':'auf Linie'}.`);\n  return{ok:true,toolpath:led.toolpath,errors:[],warnings,candidates:all,selected:chosen,eligibleSideFaceIds,selectedEdgeIds:effective.edgeIds};",
"  const entered=operation.topology==='open'?applyOpenContourRampEntry(tabbed.toolpath,operation,startZ):{toolpath:tabbed.toolpath,errors:[] as string[],warnings:[] as string[]};errors.push(...entered.errors);warnings.push(...entered.warnings);if(errors.length)return fail(errors,warnings,all,chosen,eligibleSideFaceIds,effective.edgeIds);\n  const led=operation.topology==='closed'?applyContourLeads(entered.toolpath,operation):{toolpath:entered.toolpath,errors:[] as string[],warnings:[] as string[]};errors.push(...led.errors);warnings.push(...led.warnings);if(errors.length)return fail(errors,warnings,all,chosen,eligibleSideFaceIds,effective.edgeIds);\n  if(operation.topology==='open')warnings.push(`Offene STEP-Kontur aktiv · ${effective.edgeIds.length} BRep-Kanten · ${operation.openSide==='left'?'links':operation.openSide==='right'?'rechts':'auf Linie'}.`);\n  return{ok:true,toolpath:led.toolpath,errors:[],warnings,candidates:all,selected:chosen,eligibleSideFaceIds,selectedEdgeIds:effective.edgeIds};",'open ramp pipeline']
]);

const app='src/App.svelte';let a=fs.readFileSync(app,'utf8');
const old=`<div class="placement-section"><p class="placement-title">Ein-/Ausfahrt</p><div class="placement-grid two"><button class:active={(operation.leadMode??'none')==='none'} onclick={()=>updateContour({leadMode:'none'})}>Aus</button><button class:active={(operation.leadMode??'none')==='line'} onclick={()=>updateContour({leadMode:'line'})}>Tangential</button></div>{#if (operation.leadMode??'none')==='line'}<label>Einfahrt <input type="number" min="0.1" step="0.5" value={operation.leadInLengthMm??3} oninput={updateContourLeadInLength}/> mm</label><label>Ausfahrt <input type="number" min="0.1" step="0.5" value={operation.leadOutLengthMm??3} oninput={updateContourLeadOutLength}/> mm</label><p class="note">Der Fräser fährt linear tangential in die geschlossene Kontur ein und nach dem letzten Schnitt tangential wieder aus. Haltestege werden davor aufgelöst; die Ein-/Ausfahrt wird daher nicht an jedem Haltesteg wiederholt.</p>{/if}</div>`;
const neu=`{#if importSummary.kind==='step'&&operation.topology==='open'}<div class="placement-section"><p class="placement-title">Einfahrt</p><div class="placement-grid two"><button class:active={(operation.leadMode??'none')==='none'} onclick={()=>updateContour({leadMode:'none'})}>Aus</button><button class:active={(operation.leadMode??'none')==='line'} onclick={()=>updateContour({leadMode:'line'})}>Rampe</button></div>{#if (operation.leadMode??'none')==='line'}<label>Rampenlänge <input type="number" min="0.1" step="0.5" value={operation.leadInLengthMm??3} oninput={updateContourLeadInLength}/> mm</label><p class="note">Die Rampe bleibt vollständig auf der korrigierten Nutbahn. Sie senkt von der vorherigen Z-Ebene entlang der Kontur auf die neue Zustelltiefe ab, bereinigt den Rampenabschnitt auf Soll-Z zurück zum Start und fährt danach die komplette offene Kontur. Am Ende folgt der normale Retract auf Sicherheits-Z.</p>{/if}</div>{:else}<div class="placement-section"><p class="placement-title">Ein-/Ausfahrt</p><div class="placement-grid two"><button class:active={(operation.leadMode??'none')==='none'} onclick={()=>updateContour({leadMode:'none'})}>Aus</button><button class:active={(operation.leadMode??'none')==='line'} onclick={()=>updateContour({leadMode:'line'})}>Tangential</button></div>{#if (operation.leadMode??'none')==='line'}<label>Einfahrt <input type="number" min="0.1" step="0.5" value={operation.leadInLengthMm??3} oninput={updateContourLeadInLength}/> mm</label><label>Ausfahrt <input type="number" min="0.1" step="0.5" value={operation.leadOutLengthMm??3} oninput={updateContourLeadOutLength}/> mm</label><p class="note">Der Fräser fährt linear tangential in die geschlossene Kontur ein und nach dem letzten Schnitt tangential wieder aus. Haltestege werden davor aufgelöst; die Ein-/Ausfahrt wird daher nicht an jedem Haltesteg wiederholt.</p>{/if}</div>{/if}`;
if(!a.includes(old))throw new Error('App lead UI anchor');a=a.replace(old,neu);fs.writeFileSync(app,a);

const gate='scripts/check-004z-contracts.mjs';let g=fs.readFileSync(gate,'utf8');
const oldGate=`const contourLeads=read('src/lib/contourLeads.ts');\nrejectText(contourLeads,"operation.topology!=='closed'",'004Z-E must not reject open STEP contours');\nrequireText(contourLeads,'function applyLeadToRun','004Z-E applies leads per canonical contour passage');\nrequireText(contourLeads,'vertical(leadStart,safeZMm,run.z)','004Z-E plunges at the tangent lead start instead of diagonally rapid-plunging');\nrequireText(contourLeads,'horizontal(leadStart,first,run.z)','004Z-E enters along the local first tangent');\nrequireText(contourLeads,'horizontal(last,leadEnd,run.z)','004Z-E exits along the local last tangent');\nrequireText(contourLeads,'vertical(leadEnd,run.z,safeZMm)','004Z-E retracts at the lead end before further rapids');\nrequireText(contourLeads,"operation.topology==='open'?'offene':'geschlossene'",'004Z-E reports open and closed lead semantics explicitly');`;
const newGate=`const contourLeads=read('src/lib/contourLeads.ts');\nconst openRamp=read('src/lib/openContourRampEntry.ts');\nrequireText(contourLeads,"operation.topology!=='closed'",'004Z-E2 restores tangent leads to closed contours only');\nrequireText(openRamp,'export function applyOpenContourRampEntry','004Z-E2 owns a dedicated open-groove ramp kernel');\nrequireText(openRamp,"operation.topology==='open'&&(operation.leadMode??'none')==='line'",'004Z-E2 interprets open STEP entry mode as ramp');\nrequireText(openRamp,'const prefix=rampPrefix(run.points,rampLengthMm)','ramp is derived strictly from the canonical groove path');\nrequireText(openRamp,'const z1=previousZ+(run.z-previousZ)*(b.distanceMm/prefix.lengthMm)','ramp descends continuously from previous material level to target Z');\nrequireText(openRamp,'for(let i=prefix.points.length-1;i>0;i--)','ramp prefix is cleaned back to contour start at final depth');\nrequireText(openRamp,'copy.exitSegments=undefined','open groove uses no lateral lead-out');\nrequireText(state,"operation.topology==='open'?applyOpenContourRampEntry(tabbed.toolpath,operation,startZ)",'STEP open contour consumes the ramp with reststock-aware start Z');\nrequireText(state,"operation.topology==='closed'?applyContourLeads(entered.toolpath,operation)",'closed STEP contours retain tangential leads');\nrequireText(app,'>Rampe</button>','STEP open contour exposes ramp entry');\nrequireText(app,'>Rampenlänge <input','STEP open contour exposes ramp length');\nrequireText(app,'Am Ende folgt der normale Retract auf Sicherheits-Z.','STEP open UI explains vertical exit semantics');`;
if(!g.includes(oldGate))throw new Error('gate old E assertions');g=g.replace(oldGate,newGate);g=g.replace("and 004Z-E safe tangential leads are gated.","and 004Z-E2 keeps open-groove entry on-path with reststock-aware ramps while closed contours retain tangential leads.");fs.writeFileSync(gate,g);
